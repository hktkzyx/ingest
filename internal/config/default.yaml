# ingest 全局设置
#
# 这是 ingest 首次运行时自动写出的出厂默认配置。可以随意修改、增删字段。
# 设备识别规则放在另一个文件 devices.yaml；本文件只装"工具行为"层面的设置。
#
# 字段说明：
#   gap_days  自动分段时允许的日期间隔（天）。两个相邻文件的拍摄日期相差
#             ≤ gap_days 视为同一事件；> gap_days 起新段。默认 1：只有
#             当天 + 第二天的素材会被合在一个事件里，中间空一天就分开。

version: "1"
gap_days: 1
